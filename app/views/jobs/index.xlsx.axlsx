wb = xlsx_package.workbook

wb.add_worksheet(name: "Jobs") do |sheet|
  #this is the head row of spreadsheet
  sheet.add_row Job.column_names

  @jobs.each do |job|
    sheet.add_row job.attributes.values
  end
end
