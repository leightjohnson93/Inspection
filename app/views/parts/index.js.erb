<%# index.js.erb %>
<%= smart_listing_update(:parts) %>

<%# new.js.erb %>
<%= smart_listing_item :parts, :new, @part, "parts/form" %>

<%# create.js.erb %>
<%= smart_listing_item :parts, :create, @part,
                       @part.valid? ? "parts/part" : "parts/form" %>

<%# edit.js.erb %>
<%= smart_listing_item :parts, :edit, @part, "parts/form" %>

<%# update.js.erb %>
<%= smart_listing_item :parts, :update, @part,
                       @part.valid? ? "parts/part" : "parts/form" %>

<%# destroy.js.erb %>
<%= smart_listing_item :parts, :destroy, @part %>
